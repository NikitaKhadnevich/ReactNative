const fullList = (state) => state.notesList.fullList;
const doneList = (state) => state.notesList.doneList;
const inProgressList = (state) => state.notesList.inProgressList;

export { fullList, doneList, inProgressList };
